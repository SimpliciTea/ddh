doctype
html
  head
    title= title || 'No Title Passed'
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel='stylesheet', href='css/main.css')
  body
    - var baseUrl = locals.baseUrl ? locals.baseUrl : ""
    
    // nav lives outside main container wrap 
    include nav
    div#menu-mask
    
    div#main-container
      include header
      
      block content

      include footer

    - console.log(locals)


    script(src="js/main.js", type="text/javascript")
