mixin accordion(params)
  - //Expected data format:
  - //  
  - //headerImageUrl: '',    (if empty, defaults to title)
  - //title: '',
  - //description: ''
  - //bullets: [],           (either desc. or list req.)

  section.ac-container
    each client, index in params
      div.client
        input(id='ac-'+index, name='accordion-'+index, type='checkbox')
        label(for='ac-'+index)
          img(class='client-cover', src='images/'+client.images.cover900)
          img(class='client-logo', src='images/'+client.images.logo1x, srcset='images/'+client.images.logo2x+' 2x', alt='#{client.title}')
        article.ac-small
          h2= client.title

          if client.description
            p #{client.description}
          
          if client.bullets
            ul
              each bullet in client.bullets
                li= bullet
